<Window x:Class="Emulate.Application"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Emulate"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
            <Button x:Name="Demarrer" Click="btnDemarrer_Click" Content="Demarrer" Grid.Row="0" Grid.Column="1" />
            <Button x:Name="Reprendre" Click="btnAnimal_Click" Content="Reprendre"  Grid.Row="1" Grid.Column="1" />
            <Button x:Name="Administration" Click="btnEmployee_Click" Content="Administration"  Grid.Row="2" Grid.Column="1" />
        </Grid>
    </Grid>
</Window>
